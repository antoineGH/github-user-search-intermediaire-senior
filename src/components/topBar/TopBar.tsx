import { FilterBar } from '../filterBar/FilterBar'
import { SearchBar } from '../searchBar/SearchBar'

export const TopBar = () => {
	return (
		<>
			<p>TopBar</p>
			<SearchBar />
			<FilterBar />
		</>
	)
}
